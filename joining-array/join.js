const num = [1, 2];
const joined = num.join(", ");
console.log(joined);

const sms = "hello good morning";
const part = sms.split(" ");
console.log(part);

const combine = part.join("-");
console.log(combine);
