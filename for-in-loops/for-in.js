// for-in
let details = {
  name: "dhoni",
  age: 38,
  role: "batsman-wk"
};

for (let key in details) console.log(key, details[key]);

var city = ["chennai", "mumbai"];

for (var index in city) console.log(index, city[index]);
